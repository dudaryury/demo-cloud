spring:
  application:
    name: randow-web
  cloud:
    discovery:
      enabled: true
    config:
      discovery:
        service-id: config-server
        enabled: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka:8761/eureka/