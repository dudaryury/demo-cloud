spring:
  application:
    name: randow-web
  cloud:
    discovery:
      enabled: true
    config:
      discovery:
        service-id: config-server

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/